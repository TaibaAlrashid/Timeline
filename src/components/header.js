import { Title, HeaderColor, Description } from '../styles'

const Header = () => {
    return (
    <div> 
        <HeaderColor>
        <Title>TIMELINE?</Title>
        <Description>Some of taiba's stories.</Description>
        </HeaderColor>
    </div>    
    );
};

export default Header;

